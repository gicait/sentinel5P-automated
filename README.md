# Automated Sentinel-5P Data Pipeline for Pollution Monitoring

## Usage
To execute the full processing pipeline, run execute.py. This will execute query.py, process.py, and multitemporal.py consecutively. Make sure the scripts are all in the same folder. The scripts can also be run manually one at a time.